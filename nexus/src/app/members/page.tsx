'use client'

import {sessionAdd, sessionGet} from "@/actions/authentication/session";

export default function ()
{
    // sessionAdd("congratulations", {obj: false}).then(() =>
    // {
    //     sessionGet("congratulations").then(console.log);
    // });

    return (
        <div>

        </div>
    );
}